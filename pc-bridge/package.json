{
  "name": "codex-remote-pc-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "PC-side bridge for Codex Remote Android MVP.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node dist/src/index.js",
    "check": "tsc --noEmit",
    "validate:local": "tsc && node dist/scripts/validate-local-relay.js"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=22"
  }
}

